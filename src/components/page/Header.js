import React from 'react';
import { Heading, Flex } from '@chakra-ui/react';

const Header = () => {
	return (
		<Flex alignItems='center'>
			<Heading padding={5}>InstaUnfollow</Heading>
		</Flex>
	);
};

export default Header;
